export class Livro {
  nome: string;
  autor: string;
  isbn: string;

  constructor(nome: string, autor: string, isbn: string) {
    this.nome = nome;
    this.autor = autor;
    this.isbn = isbn;
  }
}
